export enum ChainId {
  DUSANET,
  BUILDNET,
  MAINNET
}

export enum TradeType {
  EXACT_INPUT,
  EXACT_OUTPUT
}

export enum Rounding {
  ROUND_DOWN,
  ROUND_HALF_UP,
  ROUND_UP
}

export enum SolidityType {
  uint8 = 'uint8',
  uint256 = 'uint256'
}

export const ZERO_HEX = '0x0'
