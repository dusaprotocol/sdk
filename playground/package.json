{
  "name": "@dusalabs/sdk-playground",
  "license": "MIT",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "cd .. && npm run build && npm link && cd playground && npm link @dusalabs/sdk",
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "ts-node": "^10.9.1"
  },
  "engines": {
    "node": ">=10"
  }
}