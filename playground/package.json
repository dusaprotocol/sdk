{
  "name": "@dusalabs/sdk-playground",
  "license": "MIT",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "postinstall": "cd .. && npm run build && pnpm link --global && cd playground && pnpm link --global @dusalabs/sdk",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "tsx": "^4.7.2"
  },
  "engines": {
    "node": ">=10"
  }
}